// CartWidget code
import React from 'react';

const CartWidget = () => {
  return (
    <div>
      <img src=""/>{/* CartWidget icon */}
      <span>3</span> {/* Hardcoded item count */}
    </div>
  );
};

export default CartWidget;
